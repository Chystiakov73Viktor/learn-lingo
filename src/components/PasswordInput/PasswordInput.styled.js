import styled from 'styled-components';

export const WrapperDiv = styled.div`
  position: relative;
  .iconWrapper {
    position: absolute;
    top: 50%;
    right: 18px;
    transform: translateY(-50%);
  }
`;
